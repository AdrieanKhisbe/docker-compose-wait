[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
pytest = "*"

[packages]
docker-compose = ">=1.20.0"
PyYAML = ">=3.12"

[requires]
python_version = "3.6"

[scripts]
test = "pytest"
clean = "rm -rf build dist docker_compose_wait.egg-info .pytest_cache"
